<%= form_for @login_form, :url => login_functions_url do |log| %>
	<fieldset class="mystyle">
	<%if !@mess.nil?%>
		<b><%=@mess%></b><br/><br/>
	<%end%>
		<legend>Log In Internal</legend>
		<b>Ruby Version 1.45 &nbsp;&nbsp;May 29&nbsp;11:50 pm</b><br/>
 		<table class="mystyle" border="1">
			<tr  class="cell" border="1">
				<td><%=log.label :name %></td>
				<td>
					<%=log.text_field  :name, :size =>30 %>
				</td>
			</tr>
			<tr class="cell" border="1">
				<td><%= log.label :password %></td>
				<td>
					<%= log.password_field :password, :size => 30 %>
				</td>
			</tr>
				

			<tr style="padding-top:10px"  class="cell" border="1">
				<td>&nbsp;</td>
				<td><%= log.submit "Log In" %></td>
			</tr>
		</table>
	<br/><br/>
	<b>There is a schedule tab in Main Menu. Calls to New Estimates are limited to clients with prices that have not booked yet.</b><br/>
	<b>Call List shows up with estimates with zero calls followed by Pendings followed by requested list. Calls to clients with </b><br/>
	<b>zero calls is a priority. If your list skips, this means there are no prices or a job is sold already.</b>
	<b>Make Sale and Schedule show area options.</b>
	</fieldset>
<% end%>
